import producers from "../mocks/producersMock";
import header from "../mocks/headerMock";

// inferface mock data call
// requesting mockdata or maybe some API data call
export const LoadProducers = () => {
    return producers
}

export const LoadHeader = () => {
    return header
}